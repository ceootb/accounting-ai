# Evaluation

How we measure whether the model is actually good at accounting.
`tasks.jsonl` holds held-out test cases (same schema as data). A grader checks journal entries balance and answers match.
